# Hello Flutter Web

A minimal Hello World page built with Flutter.

## Run locally

Install the Flutter SDK and ensure `flutter` is available on your PATH. Then run:

```powershell
flutter pub get
flutter run -d chrome
```

To create a production web build:

```powershell
flutter build web
```
